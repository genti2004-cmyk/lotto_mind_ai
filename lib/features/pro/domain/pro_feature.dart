/// Funktionsgruppen, die über Produktstufen freigeschaltet werden können.
///
/// Der Name bleibt aus Kompatibilitätsgründen `ProFeature`, die konkrete
/// Freischaltung erfolgt aber über Normal / Pro / Premium.
enum ProFeature {
  unlimitedHistoryImport,
  advancedAnalysisProfiles,
  pdfExport,
  premiumAnalysis,
  cloudBackup,
  trackingPro,
  systemGenerator,
  strategyComparison,
  expertModels,
}
